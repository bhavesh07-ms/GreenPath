server:
  port: 8082

spring:
  application:
    name: codeflowdb


eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761
    register-with-eureka: true
    fetch-registry: true
    preferIpAddress: true